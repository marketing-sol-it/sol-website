# Only needed for the admin in production (Keystatic GitHub mode).
# Create a GitHub App via /keystatic on first run, or see README.
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=
